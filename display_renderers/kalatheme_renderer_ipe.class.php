<?php

/**
 * Renderer class for all In-Place Editor (IPE) behavior.
 */
class kalatheme_renderer_ipe extends panels_renderer_ipe {
  // The IPE operates in normal render mode, not admin mode.
  var $admin = FALSE;

  /**
   * AJAX entry point to add a new pane.
   */
  function ajax_add_col($region = NULL, $type_name = NULL, $subtype_name = NULL, $step = NULL) {
    $test == $test;
  }
}

